class Solution {
public:
    int scoreOfString(string s) {
        int sum_ans=0;
        for(int i=0;i<s.size()-1;i++){
            sum_ans+=abs(int(s[i])-int(s[i+1]));
        }
        return sum_ans;
    }
};
//to get the ascii values we do int(ch) typecast to integer
